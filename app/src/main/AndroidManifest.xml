<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pdm.ownyourvet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ownyourvet"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ownyourvet"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AdminClientInfo" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MainEmptyActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ForgotPasswordActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/NoPreview"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>